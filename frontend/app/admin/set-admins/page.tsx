'use client';
import { Plus, Search, ShieldCheck } from 'lucide-react'; import { PageHeading } from '@/components/portal'; import { sets } from '@/lib/portal-data';
export default function SetAdmins(){return <><PageHeading eyebrow="Role management" title="Set administrators" copy="Onboard trusted administrators and restrict each account to its assigned graduating set." action={<button className="portal-primary"><Plus/>Onboard set admin</button>}/><div className="permission-note"><ShieldCheck/><span><b>Scoped permissions are enforced</b><small>A Set Admin can approve, update and manage only members belonging to the set assigned below.</small></span></div><section className="portal-card management-card"><div className="management-tools"><div className="portal-search"><Search/><input placeholder="Search administrators..."/></div></div><div className="admin-directory">{sets.filter(set=>set.admin!=='Not assigned').map(set=><article key={set.year}><span className="portal-avatar">{set.admin.split(' ').filter(n=>!n.includes('.')).map(n=>n[0]).join('').slice(0,2)}</span><div><b>{set.admin}</b><small>Administrator · Class of {set.year}</small></div><em>Active</em><button>Manage access</button></article>)}</div></section></>}
