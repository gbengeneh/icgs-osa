'use client';
import { Search } from 'lucide-react'; import { PageHeading } from '@/components/portal'; import { recentMembers } from '@/lib/portal-data';
export default function Members(){return <><PageHeading eyebrow="Membership directory" title="Manage members" copy="Review applications and maintain verified alumni records."/><section className="portal-card management-card"><div className="management-tools"><div className="portal-search"><Search/><input placeholder="Search members..."/></div><button className="portal-primary">Export directory</button></div><div className="member-table"><div className="table-row table-head"><span>Member</span><span>Set</span><span>Location</span><span>Status</span></div>{recentMembers.map(row=><div className="table-row" key={row[0]}><span><i>{row[0].split(' ').map(n=>n[0]).join('')}</i><b>{row[0]}</b></span><span>{row[1]}</span><span>{row[2]}</span><span><em className={row[3].toLowerCase()}>{row[3]}</em></span></div>)}</div></section></>}
