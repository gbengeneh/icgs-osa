'use client';
import Link from 'next/link'; import { FormEvent, useEffect, useState } from 'react'; import { api, ApiError } from '@/lib/api';
type AlumniSet={id:number;year:number};
export default function Register(){const [sets,setSets]=useState<AlumniSet[]>([]);const [error,setError]=useState('');const [success,setSuccess]=useState('');const [busy,setBusy]=useState(false);useEffect(()=>{api<AlumniSet[]>('/sets').then(setSets).catch(()=>setError('Unable to load graduating sets. Please try again.'))},[]);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError('');const form=new FormData(event.currentTarget);try{await api('/register',{method:'POST',body:JSON.stringify(Object.fromEntries(form))});setSuccess('Application submitted. Your Set Administrator will review and approve your membership.');event.currentTarget.reset()}catch(reason){const err=reason as ApiError;setError(err.errors?Object.values(err.errors).flat()[0]:err.message)}finally{setBusy(false)}}return <section className="auth-wrap"><div className="auth-art"><span className="eyebrow light-text">Join the family</span><h2>One school.<br/>Every generation.</h2><p>Create your profile and reconnect with the people and place that shaped your story.</p></div><div className="auth-form"><span className="eyebrow">Membership application</span><h1>Join ICGS-OSA</h1><p className="muted">Your application goes directly to the administrator of your graduating set.</p>{error&&<div className="form-message error">{error}</div>}{success?<div className="form-message success">{success}<Link href="/login">Continue to sign in →</Link></div>:<form onSubmit={submit}><div className="field-grid"><div className="field"><label htmlFor="name">Full name</label><input id="name" name="name" required placeholder="Your full name"/></div><div className="field"><label htmlFor="graduating_year">Graduating set</label><select id="graduating_year" name="graduating_year" required defaultValue=""><option value="" disabled>Select your set</option>{sets.map(set=><option key={set.id} value={set.year}>Class of {set.year}</option>)}</select></div></div><div className="field"><label htmlFor="register-email">Email address</label><input id="register-email" name="email" required type="email" placeholder="you@example.com"/></div><div className="field-grid"><div className="field"><label htmlFor="phone">Phone number</label><input id="phone" name="phone" placeholder="+234..."/></div><div className="field"><label htmlFor="location">Current location</label><input id="location" name="location" placeholder="City, Country"/></div></div><div className="field-grid"><div className="field"><label htmlFor="register-password">Password</label><input id="register-password" name="password" required minLength={8} type="password" placeholder="Minimum 8 characters"/></div><div className="field"><label htmlFor="password_confirmation">Confirm password</label><input id="password_confirmation" name="password_confirmation" required minLength={8} type="password" placeholder="Repeat password"/></div></div><button className="button" disabled={busy}>{busy?'Submitting...':'Submit application'}</button></form>}<p className="muted">Already registered? <Link className="arrow-link" href="/login">Sign in</Link></p></div></section>}
