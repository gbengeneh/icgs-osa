# Leave unset for the recommended same-origin /backend-api proxy.
# NEXT_PUBLIC_API_URL=https://api.example.com/api
# Server-only origin for the same-origin proxy. Set this on Vercel to the
# stable Cloudflare Tunnel hostname, for example https://api.example.com
BACKEND_ORIGIN=http://127.0.0.1:8000
