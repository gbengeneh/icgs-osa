'use client';
import Link from 'next/link'; import { CalendarDays, MapPin, X } from 'lucide-react'; import { useEffect, useState } from 'react'; import { api } from '@/lib/api'; import type { PublicEvent } from '@/app/events/page';
export function EventFlash(){const [event,setEvent]=useState<PublicEvent|null>(null);const [visible,setVisible]=useState(true);useEffect(()=>{api<PublicEvent|null>('/events/featured').then(setEvent).catch(()=>setEvent(null))},[]);if(!event||!visible)return null;return <section className="event-flash"><div className="shell"><span className="flash-label">Don’t miss it</span><div><b>{event.title}</b><span><CalendarDays/> 3–6 September 2026</span><span><MapPin/> {event.location}</span></div><Link href="/events">View event</Link><button onClick={()=>setVisible(false)} aria-label="Dismiss event announcement"><X/></button></div></section>}
